# Local Skills

Local agent skills specific to the `sdkwork-settings` repository.

Add skills here when repository-specific agent extensions are needed. Each skill lives in its own directory with a `SKILL.md` file describing inputs, workflow, rules, and verification.
